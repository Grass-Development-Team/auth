use crate::internal::serializer::common::Response;
use axum::Json;

/// User register
pub async fn register() -> Json<Response> {
    todo!("Register controller");
}

/// User login
pub async fn login() -> Json<Response> {
    todo!("Login controller");
}